# Airtable — used in lib/content.ts to fetch partner logos at build time.
# Copy this file to .env.local and fill in real values.
# .env.local is gitignored; never commit secrets.

AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=
LOGOWALL_TABLE=
